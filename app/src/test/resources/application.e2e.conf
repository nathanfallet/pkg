ktor {
    deployment {
        port = 8080
    }
    application {
        modules = []
    }
    environment = "test"
}
database {
    protocol = "h2"
    host = ""
    # Its own in-memory database, so the end to end run never shares rows with the other tests.
    name = "pkge2e"
    user = ""
    password = ""
}
storage {
    path = "build/test-storage"
}
jwt {
    secret = "test"
    issuer = "pkg"
    audience = "pkg"
}
s3 {
    region = ""
    name = ""
    key = ""
    secret = ""
}
